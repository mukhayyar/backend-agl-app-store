# Environment — never commit secrets
.env

# Rendered configs (generated by deploy.sh)
Caddyfile
docker/flat-manager/config.json

# TLS certificates
certs/

# flat-manager binary (arch-specific)
docker/flat-manager/flat-manager

# Node
node_modules/
.next/
dist/

# Python
__pycache__/
*.pyc
.venv/

# Logs
*.log

# Pre-built binaries (arch-specific, copied by setup.sh)
docker/flat-manager/flat-manager-client
docker/flat-manager/gentoken
.env.bak
*.env.bak
